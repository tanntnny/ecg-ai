# @package _global_

defaults:
  - override /model: lead_encoder_base
  - override /train: single_gpu_l

model:
  lead: "aVL"
  task: "age"

train:
  metrics: ["mse", "pred_dist"]
  epochs: 50
  batch_size: 64

notes: "Train the Lead aVL Encoder model for age prediction task."