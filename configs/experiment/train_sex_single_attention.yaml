# @package _global_

defaults:
  - override /model: ecg_base
  - override /train: single_gpu_l

model:
  encoder: "cnn"
  task: "sex"

train:
  trainer: "hf"
  metrics: ["accuracy", "pred_dist", "f1", "precision", "recall"]
  epochs: 50
  batch_size: 16
  metric_for_best_model: "eval_loss"
  greater_is_better: false