# @package _global_

defaults:
  - override /model: lead_encoder_base
  - override /train: single_gpu_l

model:
  task: "age"

train:
  trainer: "lead_trainer"
  metrics: ["mse", "pred_dist"]
  epochs: 50
  batch_size: 128
  metric_for_best_model: "eval_loss"
  greater_is_better: false